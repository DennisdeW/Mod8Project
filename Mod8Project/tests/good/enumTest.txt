program enumTest;

enum Days : MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY;

def int main() {
	Days t = Days.TUESDAY;
	Days w = Days.WEDNESDAY;
	Days[] weekend = [Days.SATURDAY, Days.SUNDAY];
	Days* dayPtr = &t;
	*dayPtr = Days.THURSDAY;
	if (t != Days.THURSDAY) {
		out(1);
	} else if (weekend[1] != Days.SUNDAY) {
		out(2);
	} else {
		out(0);
	}
	return 0;
}